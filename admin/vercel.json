{
  "builds": [
    {
      "src": "admin/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "admin/dist"
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/admin/$1" }
  ]
}
